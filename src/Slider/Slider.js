import React from "react";
import { Box, Container, Typography } from "@mui/material";
import useStyles from "./style";

const Slider = () => {
  const {} = useStyles();
  const items = [1, 2, 3, 4, 5, 6];
  return (
    <Container sx={{ width: "100px", bgcolor: "red" }}>
      <Box>{}</Box>
    </Container>
  );
};

export default Slider;
